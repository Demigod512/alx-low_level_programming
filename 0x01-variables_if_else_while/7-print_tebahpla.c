#include <stdio.h>
/**
*main -Entry point
*Discription: 'check for number if its positive or negative'
*Return: always 0
*/
int main(void)
{
	int a = 122;

	while (a >= 97)
	{
		putchar(a);
		a--;
	}
	putchar('\n');
	return (0);
}
